import React from 'react';

// Project Imports
import UpdateEventView from '@/views/events/UpdateEventView';

// Metadata
export const metadata = {
  title: 'XicoNemi | Actualizar Evento',
  description: 'Actualizar un evento en la plataforma',
};

export default function UpdateEventPage(): React.JSX.Element {
  return <UpdateEventView />;
}
